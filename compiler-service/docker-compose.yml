services:
  compiler:
    build: .
    image: esp32-compiler:latest
    container_name: esp32-compiler
    restart: unless-stopped
    ports:
      - "8760:8760"
    # Compiler is CPU/memory heavy 鈥?limit resources
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4g

