services:
  compiler:
    build: .
    image: esp32-compiler:latest
    container_name: esp32-compiler
    restart: unless-stopped
    ports:
      - "8766:8765"
    # Compiler is CPU/memory heavy — limit resources
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4g
